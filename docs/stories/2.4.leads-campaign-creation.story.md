# Story 2.4: Leads Campaign Creation (Landing Page)

## Status

Done

## Executor Assignment

```yaml
executor: "@dev"
quality_gate: "@architect"
quality_gate_tools: ["lint", "typecheck", "test"]
```

## Story

**As a** gestor de tráfego,
**I want** criar uma campanha de Leads que direciona para minha landing page,
**so that** eu possa captar leads sem configurar manualmente no Gerenciador.

## Acceptance Criteria

1. Comando `meta-ads create leads` inicia fluxo de criação de campanha de Leads
2. Sistema solicita: nome do anúncio, orçamento diário (R$), URL da landing page, textos (título, texto principal, descrição)
3. Cria campanha com objetivo `OUTCOME_LEADS`
4. Cria ad set com Advantage+ audience, orçamento diário, destination type `WEBSITE`
5. Cria ad com criativo, textos, URL da landing page, página do Facebook e Instagram padrão
6. Aplica nomenclatura automática: `PPT_LEADS_LP_[DD-MM-AA]_[NOME]`
7. Ativa a campanha (status `ACTIVE`) automaticamente
8. Exibe resumo final com link para Gerenciador de Anúncios
9. Testes de integração cobrindo fluxo completo com mocks

## 🤖 CodeRabbit Integration

> **CodeRabbit Integration**: Disabled
>
> CodeRabbit CLI is not enabled in `core-config.yaml`.
> Quality validation will use manual review process only.
> To enable, set `coderabbit_integration.enabled: true` in core-config.yaml

## Tasks / Subtasks

- [x] **Task 1: Create Leads Campaign Strategy** (AC: 3, 4, 5)
  - [x] Create `src/campaign/strategies/leads.strategy.ts` implementing `CampaignStrategy`
    - Campaign: `objective: 'OUTCOME_LEADS'`, `special_ad_categories: []`, `status: 'PAUSED'`
    - Ad Set: `optimization_goal: 'LANDING_PAGE_VIEWS'`, `billing_event: 'IMPRESSIONS'`, `destination_type: 'WEBSITE'`, `targeting: { geo_locations: { countries: ['BR'] } }` (Advantage+ defaults), `daily_budget` in centavos (multiply by 100)
    - Ad: `creative` with `object_story_spec` (page_id, link_data with image_hash or video_id, headline, body, description, call_to_action `LEARN_MORE`, link = landingPageUrl)
  - [x] Export class `LeadsCampaignStrategy`

- [x] **Task 2: Add Zod validation for leads type** (AC: 2, 3)
  - [x] In `src/types/campaign.ts`, add a `.refine()` to `campaignConfigSchema`:
    - When `type === 'leads'`, `landingPageUrl` must not be null
    - Error message: `'URL da landing page é obrigatória para campanhas de leads'`
    - Path: `['landingPageUrl']`

- [x] **Task 3: Register LeadsCampaignStrategy in Orchestrator** (AC: 3)
  - [x] In `src/campaign/orchestrator.ts`:
    - Import `LeadsCampaignStrategy` from `./strategies/leads.strategy.js`
    - Add `case 'leads': return new LeadsCampaignStrategy();` in `getStrategy()`
  - [x] In the `getAdParams` call, pass `landingPageUrl` instead of `websiteUrl` when type is leads:
    - The orchestrator currently passes `config.websiteUrl ?? ''` as `websiteUrl` to `getAdParams`
    - For leads, it should pass `config.landingPageUrl ?? ''` instead
    - Update the line: `websiteUrl: config.type === 'leads' ? (config.landingPageUrl ?? '') : (config.websiteUrl ?? '')`

- [x] **Task 4: Create CLI `create leads` command** (AC: 1, 2, 6, 7, 8)
  - [x] In `src/cli/commands/create.ts`:
    - Create `leadsCommand` (new `Command('leads')`) similar to `salesCommand`
    - Description: `'Criar campanha de Leads (Landing Page)'`
    - Argument: `[name]` — Nome do anúncio
    - Prompts:
      - Nome do anúncio (`name` argument or prompt)
      - Orçamento diário em R$ (prompt)
      - URL da landing page (prompt) — **this replaces the website URL prompt from sales**
      - Título do anúncio (prompt)
      - Texto principal (prompt)
      - Descrição (prompt)
    - Build `CampaignConfig` with `type: 'leads'`, `callToAction: 'LEARN_MORE'`, `websiteUrl: null`, `landingPageUrl: inputUrl`
    - Same pipeline: scan creatives → validate → build bundle → create campaign → show result
    - Same progress spinners (steps 1-5) and result table as sales
    - Same error handling pattern
  - [x] Register: `createCommand.addCommand(leadsCommand);`

- [x] **Task 5: Write tests for Leads Strategy** (AC: 3, 4, 9)
  - [x] Create `tests/unit/campaign/strategies/leads.strategy.test.ts`
  - [x] Test: `getCampaignParams` returns `objective: 'OUTCOME_LEADS'`
  - [x] Test: `getCampaignParams` returns `status: 'PAUSED'`
  - [x] Test: `getAdSetParams` returns `optimization_goal: 'LANDING_PAGE_VIEWS'`
  - [x] Test: `getAdSetParams` returns `destination_type: 'WEBSITE'`
  - [x] Test: `getAdSetParams` converts `dailyBudget` to centavos (multiply by 100)
  - [x] Test: `getAdSetParams` includes targeting with `geo_locations.countries: ['BR']`
  - [x] Test: `getAdParams` builds correct `object_story_spec` with `link_data`
  - [x] Test: `getAdParams` uses `call_to_action.type: 'LEARN_MORE'`
  - [x] Test: `getAdParams` includes `image_hash` when provided
  - [x] Test: `getAdParams` includes `video_id` when provided
  - [x] Test: `getAdParams` includes `instagram_actor_id` when provided

- [x] **Task 6: Write tests for leads Zod validation** (AC: 2, 9)
  - [x] In `tests/unit/types/campaign.test.ts`:
  - [x] Test: schema validates valid leads config with `landingPageUrl`
  - [x] Test: schema rejects leads config with `landingPageUrl: null` — error message in Portuguese
  - [x] Test: schema allows sales config with `landingPageUrl: null` (no regression)

- [x] **Task 7: Write tests for Orchestrator leads flow** (AC: 3, 5, 7, 9)
  - [x] In `tests/unit/campaign/orchestrator.test.ts`:
  - [x] Test: orchestrator uses `LeadsCampaignStrategy` when `type === 'leads'`
  - [x] Test: full leads pipeline executes in correct order (same 5 phases)
  - [x] Test: returns `CampaignResult` with `type: 'leads'`
  - [x] Test: passes `landingPageUrl` to strategy's `getAdParams` (not `websiteUrl`)

- [x] **Task 8: Write integration tests with MSW** (AC: 9)
  - [x] In `tests/integration/campaign-flow.test.ts`:
  - [x] Test: full leads campaign creation flow → returns CampaignResult with `type: 'leads'`
  - [x] Test: leads campaign with Meta API error → Portuguese error message
  - [x] Reuse existing MSW handlers (same endpoints as sales)

- [x] **Task 9: Write tests for CLI leads command** (AC: 1, 8, 9)
  - [x] In `tests/unit/cli/commands/create.test.ts`:
  - [x] Test: `leads` subcommand exists with correct description
  - [x] Test: createCommand has both `sales` and `leads` subcommands registered

- [x] **Task 10: Final verification**
  - [x] Run `npm run lint` — no errors
  - [x] Run `npm run build` — no errors (tsup ESM output)
  - [x] Run `npm test` — all tests pass (159 total: 28 new + 131 regression)
  - [x] Verify `node dist/bin/meta-ads.js create leads --help` displays command help
  - [x] Verify existing commands still work: `create sales --help`, `upload --help`, `creatives --help`

## Dev Notes

### Previous Story Insights (Story 2.3)

- Strategy pattern already established: `SalesCampaignStrategy` at `src/campaign/strategies/sales.strategy.ts` is the exact blueprint for creating `LeadsCampaignStrategy`
- `CampaignStrategy` interface at `src/campaign/strategies/campaign-strategy.ts` defines `getCampaignParams()`, `getAdSetParams(AdSetOptions)`, `getAdParams(AdOptions)` — implement all three
- Orchestrator at `src/campaign/orchestrator.ts` has `getStrategy()` function — just add `case 'leads'`
- CLI create command at `src/cli/commands/create.ts` — `salesCommand` is the template for `leadsCommand`
- Naming service already maps `leads` → `PPT_LEADS_LP_[date]_[name]` (no changes needed)
- `CampaignType` already includes `'leads'` — no type changes needed
- `CampaignConfig` already has `landingPageUrl: string | null` field — no interface changes needed
- Used Node.js built-in `readline/promises` for interactive prompts (zero deps, ESM compatible)
- `callToAction` was hardcoded to `SHOP_NOW` for sales — use `LEARN_MORE` for leads
- `pixelId` added as nullable in `CampaignConfig` — leads typically doesn't use pixel, pass `null`
- QA Observation #5 from Story 2.3: "Strategy pattern well-implemented for leads extension in Story 2.4"

### Leads Campaign Strategy Parameters [Source: docs/architecture.md#Campaign Strategies]

- **Campaign**: `objective: 'OUTCOME_LEADS'`, `special_ad_categories: []`
- **Ad Set**: `optimization_goal: 'LANDING_PAGE_VIEWS'`, `billing_event: 'IMPRESSIONS'`, `destination_type: 'WEBSITE'`, Advantage+ audience, `daily_budget` in centavos
- **Ad**: `object_story_spec` with `page_id`, `link_data` (image_hash/video_id, headline, body, description, `call_to_action: LEARN_MORE`, link = landingPageUrl)

[Source: docs/architecture.md#Components — Section 3. Campaign Strategies]

### Naming Convention [Source: docs/architecture.md#Naming Service]

Pattern: `PPT_LEADS_LP_[DD-MM-AA]_[NOME]` — Already implemented in `src/campaign/naming.ts`, no changes needed.

### Key Differences from Sales Strategy

| Parameter | Sales | Leads |
|-----------|-------|-------|
| Campaign objective | `OUTCOME_SALES` | `OUTCOME_LEADS` |
| AdSet optimization_goal | `OFFSITE_CONVERSIONS` | `LANDING_PAGE_VIEWS` |
| AdSet promoted_object | `{ pixel_id, custom_event_type: 'PURCHASE' }` | Not needed |
| AdSet destination_type | Not set | `'WEBSITE'` |
| Ad call_to_action | `SHOP_NOW` | `LEARN_MORE` |
| URL field used | `websiteUrl` | `landingPageUrl` |
| Naming event | `COMPRA` | `LP` |

### Source Tree [Source: docs/architecture.md#Source Tree]

```
src/campaign/strategies/
├── campaign-strategy.ts   # CampaignStrategy interface — EXISTS (no changes)
├── sales.strategy.ts      # SalesCampaignStrategy — EXISTS (no changes)
└── leads.strategy.ts      # LeadsCampaignStrategy — NEW

src/campaign/
└── orchestrator.ts        # Add 'leads' case to getStrategy() — MODIFY

src/types/
└── campaign.ts            # Add leads landingPageUrl validation — MODIFY

src/cli/commands/
└── create.ts              # Add leads subcommand — MODIFY

tests/unit/campaign/strategies/
└── leads.strategy.test.ts # NEW

tests/unit/types/
└── campaign.test.ts       # Add leads validation tests — MODIFY

tests/unit/campaign/
└── orchestrator.test.ts   # Add leads orchestrator tests — MODIFY

tests/integration/
└── campaign-flow.test.ts  # Add leads flow test — MODIFY

tests/unit/cli/commands/
└── create.test.ts         # Add leads command test — MODIFY
```

### Existing Code to Reuse

- `src/campaign/strategies/sales.strategy.ts` — Copy and adapt for leads (same interface, different params)
- `src/campaign/orchestrator.ts` — Add `case 'leads'` + adjust `websiteUrl` passing logic
- `src/cli/commands/create.ts` — Copy `salesCommand` pattern, change prompts and config
- `src/meta-api/adapter.ts` — No changes needed (same endpoints for all campaign types)
- `src/meta-api/uploader.ts` — No changes needed (same upload flow)
- `src/campaign/naming.ts` — No changes needed (already handles `leads` type)
- `tests/helpers/msw-handlers.ts` — No changes needed (same campaign endpoints)

### Error Handling [Source: docs/architecture.md#Error Handling Strategy]

Same error handling as Story 2.3 — same rollback strategy, same error codes, same Portuguese messages. No new error codes needed for leads.

### Coding Standards [Source: docs/architecture.md#Coding Standards]

- TypeScript 5.7.x strict, ESM modules
- kebab-case for files: `leads.strategy.ts`
- PascalCase for classes: `LeadsCampaignStrategy`
- Max 200 lines per file
- Async/await everywhere
- Zod for runtime validation

## Testing

- **Unit tests:** Cover leads strategy params, Zod validation for landingPageUrl, orchestrator leads flow, CLI command registration
- **Integration tests:** MSW mocking Meta API campaign endpoints for leads flow
- **Mocking strategy:** Same as Story 2.3 — mock adapter methods, uploadBundle, fs/promises, token management
- **Regression:** All 131 existing tests must continue to pass
- **Framework:** vitest 3.x, `vi.mock()`, AAA pattern
- **Test locations:** Mirror `src/` structure in `tests/unit/`, integration in `tests/integration/`

## Dev Agent Record

### Agent Model Used

Claude Opus 4.6

### Debug Log References

1. Lint: PASS — zero errors
2. Build: PASS — tsup ESM output (64.89 KB bin, 64.87 KB cli)
3. Tests: 159 passed (28 new + 131 regression), 26 test files, 1.27s
4. CLI verify: `create leads --help` displays correctly
5. Regression: `create sales --help`, `upload --help`, `creatives --help` still functional

### Completion Notes

- `LeadsCampaignStrategy` follows exact same pattern as `SalesCampaignStrategy` — same interface, different params
- Leads does NOT use `promoted_object` (no pixel_id needed, unlike sales)
- Leads adds `destination_type: 'WEBSITE'` to ad set params (sales doesn't set this)
- `callToAction` set to `LEARN_MORE` for leads (vs `SHOP_NOW` for sales)
- Orchestrator dynamically routes URL: `landingPageUrl` for leads, `websiteUrl` for sales
- Zod validation chains two `.refine()` calls — one for sales websiteUrl, one for leads landingPageUrl
- CLI `leadsCommand` follows `salesCommand` pattern exactly, swapping URL prompt and config fields

### File List
**New files:**
- `src/campaign/strategies/leads.strategy.ts` — LeadsCampaignStrategy (OUTCOME_LEADS)
- `tests/unit/campaign/strategies/leads.strategy.test.ts` — 18 tests

**Modified files:**
- `src/types/campaign.ts` — Added .refine() for leads landingPageUrl validation
- `src/campaign/orchestrator.ts` — Added LeadsCampaignStrategy import, 'leads' case, URL routing
- `src/cli/commands/create.ts` — Added leadsCommand + registered on createCommand
- `tests/unit/types/campaign.test.ts` — Added 3 leads validation tests
- `tests/unit/campaign/orchestrator.test.ts` — Added 4 leads orchestrator tests
- `tests/integration/campaign-flow.test.ts` — Added 2 leads integration tests
- `tests/unit/cli/commands/create.test.ts` — Added 2 leads CLI tests

### Change Log
- 2026-03-10: Story 2.4 drafted — Leads Campaign Creation (Landing Page)
- 2026-03-10: PO validation GO (94/100) — Status: Ready for Dev
- 2026-03-10: All 10 tasks implemented — 159 tests passing, lint/build clean
- 2026-03-10: QA Gate PASS (96/100) — All 9 ACs traced, 159 tests verified independently

## QA Results

### Gate Decision: PASS (96/100)

**Reviewer:** Quinn (QA Agent) — Claude Opus 4.6
**Date:** 2026-03-10

### AC Traceability (9/9 — 100%)

| AC | Status | Evidence |
|----|--------|----------|
| AC1 | ✅ | `leadsCommand` registered in `create.ts:158`, CLI `--help` verified |
| AC2 | ✅ | Interactive prompts in `create.ts:181-193` (nome, orçamento, URL LP, título, texto, descrição) |
| AC3 | ✅ | `leads.strategy.ts:7` returns `OUTCOME_LEADS`, unit + integration tests |
| AC4 | ✅ | `leads.strategy.ts:14-22` — Advantage+, `WEBSITE`, daily_budget centavos |
| AC5 | ✅ | `leads.strategy.ts:26-56` — object_story_spec with link_data, page, instagram |
| AC6 | ✅ | `naming.ts` already handles `leads` → `PPT_LEADS_LP_[DD-MM-AA]_[NOME]` |
| AC7 | ✅ | Orchestrator activates campaign (PAUSED→ACTIVE), integration test confirms |
| AC8 | ✅ | `create.ts:249-262` — result table with link to Ads Manager |
| AC9 | ✅ | 2 integration tests (MSW) + 26 unit tests covering leads flow |

### Independent Verification

| Check | Result | Details |
|-------|--------|---------|
| Lint | ✅ PASS | Zero errors |
| Build | ✅ PASS | tsup ESM — 64.89 KB bin, 64.87 KB cli |
| Tests | ✅ PASS | 159 tests, 26 files, 1.05s |
| CLI `create leads --help` | ✅ PASS | Displays correctly |
| Regression | ✅ PASS | `sales`, `upload`, `creatives` commands functional |

### Code Quality Assessment

1. **Strategy pattern** — LeadsCampaignStrategy follows SalesCampaignStrategy blueprint exactly. Clean separation of concerns.
2. **No promoted_object** — Correctly omitted for leads (unlike sales which requires pixel_id).
3. **destination_type: 'WEBSITE'** — Correctly added for leads (sales doesn't set this).
4. **URL routing** — Orchestrator dynamically passes `landingPageUrl` for leads, `websiteUrl` for sales.
5. **Zod validation** — Chained `.refine()` validates type-specific URL requirements without regression.
6. **Test coverage** — 28 new tests (18 strategy + 3 validation + 4 orchestrator + 2 integration + 1 CLI) = comprehensive.
7. **No regressions** — All 131 existing tests continue to pass.

### Observations

- No issues found. Implementation is clean, minimal, and follows established patterns.
- File count within limits (200 lines max per file respected).
- All error handling follows existing Portuguese message patterns.

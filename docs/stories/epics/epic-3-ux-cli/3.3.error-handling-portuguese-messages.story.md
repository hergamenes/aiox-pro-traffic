# Story 3.3: Error Handling & Portuguese Messages

## Status

Ready for Review

## Executor Assignment

```yaml
executor: "@dev"
quality_gate: "@architect"
quality_gate_tools: ["lint", "typecheck", "test"]
```

## Story

**As a** gestor de tráfego,
**I want** mensagens de erro claras em português quando algo der errado,
**so that** eu saiba exatamente o que corrigir sem precisar pesquisar códigos de erro.

## Acceptance Criteria

1. Mapeamento dos 20 erros mais comuns da Meta API para mensagens em português com ação sugerida
2. Exemplos de mensagens claras com ações sugeridas: `"Imagem muito pequena (800x600). Mínimo: 1080x1080."`, `"Token expirado. Execute: meta-ads auth setup"`, `"Orçamento abaixo do mínimo da Meta (R$5/dia)."`
3. Erros de rede tratados: timeout, sem internet, DNS failure — com sugestão de retry
4. Erros inesperados salvos em log com stack trace para debug, mensagem amigável para o usuário
5. Validação prévia (antes de chamar API): criativos, orçamento mínimo, token válido, página configurada
6. Testes unitários para cada mapeamento de erro

## CodeRabbit Integration

> **CodeRabbit Integration**: Disabled
>
> CodeRabbit CLI is not enabled in `core-config.yaml`.
> Quality validation will use manual review process only.
> To enable, set `coderabbit_integration.enabled: true` in core-config.yaml

## Tasks / Subtasks

- [x] **Task 1: Create error-map module** (AC: 1, 2)
  - [x] Create `src/errors/error-map.ts` with `META_ERROR_MAP` constant
  - [x] Map 20+ Meta API error codes to Portuguese messages + suggested actions
  - [x] Include codes: 1 (unknown), 2 (unexpected), 4 (rate limit), 10 (permission), 17 (account limit), 100 (invalid param), 190 (token expired), 200 (permission), 294 (title too long), 368 (blocked account), 506 (duplicate), 1487390 (ad review), 2446 (creative rejected), 2635 (budget too low), 32 (page unavailable), 803 (resource not found), 1815149 (targeting error), 275 (read-only mode), 80004 (rate limit ads), 900 (campaign limit)
  - [x] Export `translateMetaError(code: number, detail?: string): { message: string; action: string }` function

- [x] **Task 2: Create error-handler module** (AC: 3, 4)
  - [x] Create `src/errors/error-handler.ts` with centralized error handling
  - [x] Function `handleError(error: unknown): { message: string; action: string; shouldLog: boolean }` — translates any error to user-friendly output
  - [x] Network error detection: `ENOTFOUND` (DNS), `ETIMEDOUT` (timeout), `ECONNREFUSED` (connection refused), `ECONNRESET` (connection reset), `ERR_NETWORK` → Portuguese messages with retry suggestion
  - [x] Unexpected errors: log full stack trace via `logger.error()`, return generic Portuguese message `"Erro inesperado. Detalhes salvos em log."`

- [x] **Task 3: Refactor adapter.ts to use error-map** (AC: 1)
  - [x] In `src/meta-api/adapter.ts`: remove inline `META_ERROR_MAP` (lines 20-53)
  - [x] Import `translateMetaError` from `../errors/error-map.js`
  - [x] Update `handleMetaError()` to use `translateMetaError()` instead of inline map lookup
  - [x] Add network error codes: `ECONNREFUSED`, `ECONNRESET` to network error detection

- [x] **Task 4: Add pre-API validation** (AC: 5)
  - [x] Create `src/errors/pre-validators.ts` with validation functions
  - [x] Function `validatePreConditions(config: CampaignConfig, bundle: CreativeBundle): void` — throws ValidationError if preconditions fail
  - [x] Validations: token exists (via token-manager), adAccountId configured, pageId configured, bundle has assets, dailyBudget > 0
  - [x] Call `validatePreConditions()` in orchestrator before pipeline starts

- [x] **Task 5: Integrate error-handler in CLI commands** (AC: 4)
  - [x] In `src/cli/commands/up.ts`: replace inline error catch with `handleError()` from error-handler
  - [x] In `src/cli/commands/create.ts`: same replacement for both salesCommand and leadsCommand
  - [x] Ensure unexpected errors log stack trace and show friendly message

- [x] **Task 6: Write unit tests for error-map** (AC: 6)
  - [x] Create `tests/unit/errors/error-map.test.ts`
  - [x] Test: `translateMetaError()` returns correct message for each of 20 mapped codes
  - [x] Test: unknown error code returns generic message
  - [x] Test: detail parameter is interpolated when provided

- [x] **Task 7: Write unit tests for error-handler** (AC: 3, 4, 6)
  - [x] Create `tests/unit/errors/error-handler.test.ts`
  - [x] Test: network errors (ENOTFOUND, ETIMEDOUT, ECONNREFUSED, ECONNRESET) return Portuguese messages
  - [x] Test: MetaApiError is translated correctly
  - [x] Test: unexpected errors return generic message and set shouldLog=true

- [x] **Task 8: Write unit tests for pre-validators** (AC: 5, 6)
  - [x] Create `tests/unit/errors/pre-validators.test.ts`
  - [x] Test: throws ValidationError when adAccountId missing
  - [x] Test: throws ValidationError when pageId missing
  - [x] Test: throws ValidationError when bundle has no assets
  - [x] Test: throws ValidationError when dailyBudget <= 0
  - [x] Test: passes when all preconditions met

- [x] **Task 9: Final verification**
  - [x] Run `npm run lint` — no errors
  - [x] Run `npm run build` — no errors (tsup ESM output)
  - [x] Run `npm test` — all tests pass (248 tests, 33 files)
  - [x] Verify error messages display correctly: `node dist/bin/meta-ads.js up --help`
  - [x] Verify existing commands still work: `create sales --help`, `create leads --help`

## Dev Notes

### Previous Story Insights (Story 3.2)

- `up.ts` and `create.ts` already catch `ValidationError`, `CreativeError`, `UploadError`, `MetaApiError`, `NetworkError` and display `COLORS.RED` messages
- `progress.ts` provides shared `COLORS` constant for consistent ANSI output
- `adapter.ts` already has an inline `META_ERROR_MAP` with 8 error codes (lines 20-53) — needs extraction and expansion
- `handleMetaError()` in `adapter.ts` (lines 61-90) already handles basic error translation and network errors
- `logger.ts` uses pino 9.x with redaction for sensitive fields — reuse for stack trace logging

### What Already Exists vs What's New

**Already Implemented:**
- [Source: `src/errors/types.ts`] — Error class hierarchy: `AppError`, `AuthError`, `ValidationError`, `MetaApiError`, `NetworkError`, `CreativeError`, `UploadError`
- [Source: `src/meta-api/adapter.ts` lines 20-53] — 8 Meta error codes mapped (190, 100, 4, 10, 2446, 368, 2635, 17)
- [Source: `src/meta-api/adapter.ts` lines 61-90] — `handleMetaError()` with basic network detection (ENOTFOUND, ETIMEDOUT)
- [Source: `src/cli/logger.ts`] — pino logger with redaction configured
- [Source: `src/errors/types.ts` lines 70-91] — `AUTH_ERRORS` constants with Portuguese messages
- [Source: `src/utils/retry.ts`] — Retry logic with exponential backoff already implemented

**New Implementation Required:**
- AC1: Extract and expand error map from 8 to 20+ codes → `error-map.ts`
- AC3: Add more network error types (ECONNREFUSED, ECONNRESET) → `error-handler.ts`
- AC4: Centralized error handler with stack trace logging → `error-handler.ts`
- AC5: Pre-API validation module → `pre-validators.ts`
- AC6: Unit tests for all new modules

### Existing Code to Reuse [Source: docs/architecture.md#Components]

- **`pino` 9.x** — Already installed, configured with redaction in `logger.ts`
- **`types.ts`** — Error hierarchy already defined, no changes needed
- **`retry.ts`** — Exponential backoff already working for 5xx errors
- **`adapter.ts`** — `handleMetaError()` function to refactor (not rewrite)
- **`COLORS`** at `src/cli/progress.ts` — ANSI codes for error display

### Technology Context [Source: docs/architecture.md#Tech Stack]

- **`pino` 9.x** — Already installed. Use `logger.error({ err, stack })` for stack traces.
- No new dependencies needed — this story uses only existing packages.

### Coding Standards [Source: docs/architecture.md#Coding Standards]

- TypeScript 5.7.x strict, ESM modules
- kebab-case for files: `error-map.ts`, `error-handler.ts`, `pre-validators.ts`
- camelCase for functions: `translateMetaError`, `handleError`, `validatePreConditions`
- Max 200 lines per file
- Async/await everywhere
- Rule 4: "Errors never reach user raw" — always pass through ErrorHandler

### Key Implementation Notes

- **DO NOT modify `types.ts`** — error hierarchy is complete, no new classes needed
- **DO NOT modify the orchestrator pipeline logic** — only add `validatePreConditions()` call at the start
- **`translateMetaError()` signature:** `(code: number, detail?: string) => { message: string; action: string }` — the `detail` param allows inserting API-specific detail into the message template (e.g., `Parâmetro inválido: {detail}`)
- **`handleError()` must be pure** — no side effects except logging. Returns structured data for CLI to display.
- **Retry is already handled** in `retry.ts` — this story does NOT add retry logic, only better error messages
- **Error codes reference:** [Meta API Error Codes](https://developers.facebook.com/docs/marketing-api/error-reference/)

### Meta API Error Codes to Map (20+)

| Code | Portuguese Message | Suggested Action |
|------|-------------------|-----------------|
| 1 | Erro desconhecido da API Meta | Tente novamente. Se persistir, contate o suporte |
| 2 | Erro temporário da API Meta | Tente novamente em alguns minutos |
| 4 | Limite de chamadas da API atingido | Aguarde alguns minutos |
| 10 | Permissão negada | Verifique permissões do Meta App |
| 17 | Conta atingiu o limite de campanhas | Exclua campanhas antigas ou contate o suporte Meta |
| 32 | Página indisponível | Verifique se a página está publicada e ativa |
| 100 | Parâmetro inválido | Verifique os parâmetros da requisição |
| 190 | Token de acesso inválido ou expirado | Execute: meta-ads auth setup |
| 200 | Permissão insuficiente para esta operação | Verifique permissões do App e da conta |
| 275 | Conta em modo somente leitura | Verifique restrições da conta no Gerenciador |
| 294 | Título do anúncio muito longo | Reduza o título para até 40 caracteres |
| 368 | Conta temporariamente bloqueada | Acesse o Gerenciador para resolver |
| 506 | Campanha duplicada detectada | Use um nome diferente para a campanha |
| 803 | Recurso não encontrado | Verifique se o ID informado existe |
| 900 | Limite de campanhas ativas atingido | Pause ou exclua campanhas existentes |
| 1487390 | Anúncio em processo de revisão | Aguarde a revisão da Meta (até 24h) |
| 1815149 | Erro de segmentação | Verifique as configurações de público |
| 2446 | Criativo rejeitado pela Meta | Verifique políticas de anúncios da Meta |
| 2635 | Orçamento diário abaixo do mínimo | O orçamento deve ser de pelo menos R$1,00 por dia |
| 80004 | Limite de taxa para anúncios atingido | Aguarde 1 minuto antes de tentar novamente |

### Source Tree [Source: docs/architecture.md#Source Tree]

```
src/errors/
├── types.ts                  # UNCHANGED — Error class hierarchy
├── error-map.ts              # NEW — Meta API error code translations
├── error-handler.ts          # NEW — Centralized error handling
└── pre-validators.ts         # NEW — Pre-API validations

src/meta-api/
└── adapter.ts                # MODIFY — use error-map, expand network errors

src/cli/commands/
├── up.ts                     # MODIFY — use error-handler for unexpected errors
└── create.ts                 # MODIFY — use error-handler for unexpected errors

tests/unit/errors/
├── error-map.test.ts         # NEW — Error map translations
├── error-handler.test.ts     # NEW — Error handler logic
└── pre-validators.test.ts    # NEW — Pre-validation tests
```

### Error Handling [Source: docs/architecture.md#Error Handler]

No new error types needed. Existing hierarchy in `types.ts` covers all cases. The new modules organize and centralize what's already partially implemented inline.

## Testing

- **Unit tests:** Cover error-map (20+ codes), error-handler (network, unexpected), pre-validators (5 conditions)
- **Integration tests:** No new integration tests needed — existing campaign-flow tests already verify error propagation
- **Mocking strategy:** Mock `token-manager` in pre-validators tests, mock `logger` in error-handler tests
- **Regression:** All existing tests must continue to pass
- **Framework:** vitest 3.x, `vi.mock()`, AAA pattern
- **Test locations:** Mirror `src/` structure in `tests/unit/`

## Dev Agent Record

### Agent Model Used

Claude Opus 4.6

### Debug Log References

No debug issues encountered during implementation.

### Completion Notes

- Created `src/errors/error-map.ts` with 20 Meta API error codes mapped to Portuguese messages + suggested actions
- Created `src/errors/error-handler.ts` with centralized error handling: AppError passthrough, network error detection (5 codes), unexpected error logging via pino
- Created `src/errors/pre-validators.ts` with pre-API validation: token, adAccountId, pageId, bundle assets, dailyBudget
- Refactored `adapter.ts`: removed inline META_ERROR_MAP (8 codes), now uses `translateMetaError()` from error-map (20 codes), added ECONNREFUSED/ECONNRESET to network detection
- Replaced inline error catch blocks in `up.ts` and `create.ts` with centralized `handleError()` — all errors now display Portuguese message + action
- 43 new unit tests: error-map (24), error-handler (13), pre-validators (6)
- All 248 tests pass (33 files), lint clean, build clean

### File List
**New files:**
- `src/errors/error-map.ts` — Meta API error code translations (20 codes)
- `src/errors/error-handler.ts` — Centralized error handling with stack trace logging
- `src/errors/pre-validators.ts` — Pre-API validation functions
- `tests/unit/errors/error-map.test.ts` — Error map unit tests (24 tests)
- `tests/unit/errors/error-handler.test.ts` — Error handler unit tests (13 tests)
- `tests/unit/errors/pre-validators.test.ts` — Pre-validator unit tests (6 tests)

**Modified files:**
- `src/meta-api/adapter.ts` — Use error-map module, expand network error detection
- `src/cli/commands/up.ts` — Use error-handler for unexpected errors
- `src/cli/commands/create.ts` — Use error-handler for unexpected errors

### Change Log
- 2026-03-10: Story 3.3 drafted — Error Handling & Portuguese Messages
- 2026-03-10: Implementation complete — All 9 tasks done, 248 tests pass, Status: Ready for Review

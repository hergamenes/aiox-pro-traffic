# Story 8.4: `install-traffic-kit.sh` + `README-TRAFFIC-KIT.md` + branding bundle

## Status

Done

## Executor Assignment

```yaml
executor: "@dev"
quality_gate: "@qa"
quality_gate_tools: ["manual-review", "bash-smoke-test"]
```

## Story

**As a** gestor de tráfego que inicia um novo projeto de cliente,
**I want** um único comando que copia os squads, CLIs e configurações para qualquer pasta de projeto,
**so that** eu possa ter o Traffic Kit operacional num projeto novo em minutos — sem copiar arquivos manualmente, sem ajustar paths e sem risco de esquecermos algo.

## Context

Quarta e última story do **Epic 8 — Traffic Kit: Export dos Squads de Tráfego**. Depende de **Stories 8.1, 8.2 e 8.3 todas concluídas**: CLIs padronizados via PATH (8.1), `traffic-shared/` como fonte única (8.2), squads livres de dados de cliente (8.3).

Esta story entrega o empacotamento final: o script `install-traffic-kit.sh` que reproduz o ambiente em qualquer projeto, o `README-TRAFFIC-KIT.md` que documenta todo o ciclo de vida do kit, e o bundle de branding Solaro (se os assets existirem no repo).

**Princípio KISS:** installer é shell script local. Sem npm publish, sem framework de instalação, sem CI/CD. Um comando — funciona.

**Constraint crítico (isolamento):** o installer NUNCA copia `reports/` nem `data/client-profiles/` com dados reais. Ele copia os squads limpos (já garantido pelas stories 8.1-8.3).

**O que NÃO faz parte desta story:**
- Instalar/configurar o MCP Facebook (é conector da conta claude.ai — só documentar no README)
- Novas features dos squads ou CLIs
- CI/CD para o installer
- Migração automática de perfis de cliente existentes
- Publicar o kit como pacote npm

## Acceptance Criteria

1. `scripts/install-traffic-kit.sh <destino>` copia `squads/traffic-shared/`, `squads/traffic-meta/`, `squads/traffic-google/`, `.claude/commands/trafficMeta/` e `.claude/commands/trafficGoogle/` para o projeto de destino — mantendo a estrutura de diretórios.
2. O script verifica se `meta-ads` e `google-ads` estão disponíveis no PATH do sistema; se ausentes, executa build (`npm install && npm run build`) e `npm install -g .` para cada CLI a partir de `packages/meta-ads-agent/` e `packages/google-ads-agent/` respectivamente.
3. O script cria o diretório `reports/` no projeto de destino (se não existir).
4. O script faz **merge** das allow rules de LEITURA no `.claude/settings.local.json` do destino: `meta-ads` (subcomandos de leitura: `report`, `accounts`, `auth status`) e `google-ads` (idem) — adicionando apenas as regras ausentes, **nunca sobrescrevendo** regras existentes. Se o arquivo não existir, cria com as allow rules mínimas.
5. O script copia os assets de branding Solaro (logos PNG, HTML modelo) para o destino, **se** os assets existirem no repo (`reports/solaro-*` ou path identificado na implementação). A ausência de branding NÃO bloqueia a instalação.
6. `README-TRAFFIC-KIT.md` na raiz do repo documenta: (a) o que é o Traffic Kit e seus componentes, (b) instalação em projeto novo (1 comando), (c) setup em máquina nova (auth via Keychain para Meta e Google, com comandos exatos), (d) o conector MCP `claude_ai_Facebook` da conta claude.ai — o que é, onde fica, que não há nada a instalar; apenas informar o usuário, (e) como criar e usar `client-profile.md` por projeto, (f) troubleshooting (PATH/npm/nvm, permissões).
7. O installer é **idempotente**: rodar 2x no mesmo destino não duplica squads, não sobrescreve allow rules já presentes, não quebra o projeto.
8. O installer NUNCA copia `reports/` com conteúdo (a pasta `reports/` é criada vazia) nem qualquer `client-profiles/*.md` que não seja `_TEMPLATE.md`.

## CodeRabbit Integration

> **CodeRabbit Integration**: Disabled

## Tasks / Subtasks

- [x] **Task 1: Localizar assets de branding Solaro** (AC: 5)
  - [x] Grep por logos Solaro no repo: `find . -name "*.png" | grep -i solaro` e `find . -name "solaro*" -o -name "*solaro*"` (excluindo `node_modules`)
  - [x] Verificar `reports/` e subpastas para modelos HTML da Solaro
  - [x] Registrar paths encontrados (ou "não localizados") nas Completion Notes — assets existem APENAS em `reports/` (gitignored, dados de cliente). NÃO copiados; nota registrada no README e no installer

- [x] **Task 2: Criar `scripts/install-traffic-kit.sh`** (AC: 1, 2, 3, 4, 5, 7, 8)
  - [x] Criar diretório `scripts/` na raiz do repo se não existir
  - [x] Implementar o script bash com as seguintes seções:
    - [x] Validação de argumentos: verificar que `<destino>` foi passado; se não, exibir uso e sair com exit 1
    - [x] Validação de destino: verificar que o diretório de destino existe (`[ -d "$DESTINO" ]`); se não, exibir erro e sair (+ rejeita destino == repo de origem)
    - [x] Copiar squads (via `cp -R` com `rm -rf` prévio para idempotência; cria `squads/` no destino)
    - [x] Copiar shims de comandos trafficMeta/trafficGoogle (cria `.claude/commands/` no destino)
    - [x] Criar `reports/` no destino vazio com `.gitkeep`
    - [x] Verificar CLIs no PATH: `command -v meta-ads` e `command -v google-ads`; se ausentes, oferecer build + install global a partir de `packages/` (`--yes` instala direto, sem perguntar)
    - [x] Merge de allow rules no `.claude/settings.local.json` do destino (via `node` inline)
    - [x] Cópia condicional de branding: só copia se houver diretório de branding versionado FORA de `reports/`
    - [x] Idempotência: `rm -rf` + `cp -R` para squads, before-append nas allow rules
    - [x] Mensagem de conclusão com resumo do que foi instalado

- [x] **Task 3: Implementar merge de allow rules** (AC: 4, 7)
  - [x] Definir as allow rules de leitura a adicionar (formato `Bash(cmd:*)`, schema `permissions.allow` do repo)
  - [x] Implementar lógica de merge: ler JSON existente (ou `{}` se ausente), adicionar apenas regras ausentes, escrever de volta (+ backup `.bak`)
  - [x] Garantir que regras de mutação existentes (`meta-ads create`, etc.) NÃO sejam tocadas — testado: `Bash(meta-ads create:*)` preservado
  - [x] Testar merge com arquivo existente (idempotência) e com arquivo ausente (criação)

- [x] **Task 4: Criar `README-TRAFFIC-KIT.md`** (AC: 6)
  - [x] Seção: O que é o Traffic Kit (componentes)
  - [x] Seção: Instalação em projeto novo — 1 comando com exemplo literal
  - [x] Seção: Setup em máquina nova — auth Meta/Google via Keychain, comandos exatos
  - [x] Seção: MCP `claude_ai_Facebook` — o que é, nada a instalar, como confirmar no claude.ai
  - [x] Seção: Como usar client-profile por projeto (a partir do `_TEMPLATE.md`; convenção de nomenclatura)
  - [x] Seção: Troubleshooting — `command not found` (PATH/nvm/npm -g), permissões macOS, token expirado

- [x] **Task 5: Smoke test de instalação real** (AC: 1, 7, 8; valida também o DoD do epic)
  - [x] Criar diretório temporário e `git init`
  - [x] Executar o installer
  - [x] Verificar que os 3 squads foram copiados
  - [x] Verificar que os 2 shims de comandos foram copiados
  - [x] Verificar que `reports/` existe e está vazio (só `.gitkeep`)
  - [x] Verificar que `.claude/settings.local.json` tem as 6 allow rules de leitura
  - [x] Verificar que NÃO há client-profiles reais no destino (só `_TEMPLATE.md`/`README.md`)
  - [x] Executar 2x — idempotência confirmada (0 regras duplicadas, 3 squad dirs)
  - [x] Registrar resultado completo nas Completion Notes

## Dev Notes

**Contexto crítico para o @dev:**

- **Dependências obrigatórias:** Stories 8.1 + 8.2 + 8.3 todas em Done antes de executar esta story. O installer empacota o resultado dessas 3 stories — qualquer desvio nas fontes chega ao destino
- **Shell: bash, macOS/zsh-compatível.** O script deve ter shebang `#!/usr/bin/env bash` e ser executável (`chmod +x`). Testar no macOS (o usuário usa Apple Silicon)
- **Merge de allow rules — abordagem recomendada:**
  - Se `jq` disponível: `jq '.allow |= (. + $new_rules | unique)'` — idiomático e seguro
  - Se não: fallback via `node -e` inline com `JSON.parse` / `JSON.stringify` e lógica de merge manual
  - Em nenhum caso usar `echo > arquivo` (sobrescreve). Sempre ler, modificar na memória, escrever de volta
  - Allow rules de leitura a adicionar (formato a confirmar observando o `.claude/settings.local.json` atual do repo de origem):
    ```json
    {"command": "meta-ads accounts"},
    {"command": "meta-ads report"},
    {"command": "meta-ads auth status"},
    {"command": "google-ads accounts"},
    {"command": "google-ads report"},
    {"command": "google-ads auth status"}
    ```
  - Confirmar o schema exato do `settings.local.json` lendo o arquivo do próprio repo antes de implementar
- **Idempotência:** o principal risco é duplicar allow rules ou sobrescrever squads. Usar `cp -n` para arquivos e verificação de existência antes de adicionar regras resolve a maioria dos casos
- **Branding Solaro:** se localizado no Task 1, copiar para `$DESTINO/reports/branding-solaro/` (ou path equivalente já usado no repo). Se não localizado, o README documenta onde obter os assets manualmente
- **NÃO copiar:** `reports/` com conteúdo, qualquer `client-profiles/{cliente}.md` que não seja `_TEMPLATE.md`, `node_modules`, `.env`, `packages/` (o installer usa packages/ como fonte para build/install-g, mas não os copia para o destino)
- **Risco E-R4 (do epic):** installer sobrescreve `settings.local.json`. O merge via jq/node com verificação antes de append mitiga completamente. Fazer backup do arquivo existente antes de tocar: `cp settings.local.json settings.local.json.bak` é boa prática e deve ser implementado
- **Risco E-R1 (do epic):** `npm install -g` com problemas de permissão (nvm, sudo no macOS). O script deve: (a) verificar `command -v meta-ads` antes de tentar instalar, (b) se a instalação falhar, emitir mensagem clara com troubleshooting, (c) não abortar a instalação completa por causa de CLI — continuar e avisar
- **Verificar estrutura de comandos antes de copiar:** confirmar que `.claude/commands/trafficMeta/` e `.claude/commands/trafficGoogle/` existem no repo (devem existir como shims de ativação dos agentes)
- **README-TRAFFIC-KIT.md:** criar na raiz do repo (`/Users/hergamenessouza/aiox-pro-traffic/README-TRAFFIC-KIT.md`). Linguagem: português do Brasil, tom direto, instruções passo a passo numeradas (o usuário é iniciante em programação)

### Testing

| Tipo | Verificação |
|------|------------|
| Smoke de instalação | Rodar em diretório temporário vazio — confirmar todos os artefatos copiados (Task 5) |
| Smoke de idempotência | Rodar 2x no mesmo destino — sem erros, sem duplicações de allow rules |
| Smoke de segurança | Confirmar zero arquivos de dados de cliente no destino após instalação |
| Merge de allow rules | Testar com `.claude/settings.local.json` existente (regras preservadas) e sem arquivo (criado com mínimo) |

## Completion Notes

### Decisão de branding (Task 1 / AC 5)

Logos e HTML modelo da Solaro localizados APENAS dentro de `reports/`:
- `reports/medical-spin-unificado/solaro-logo-colorida.png` / `solaro-logo-branca.png`
- `reports/{verbo-feminino,sol-meta,medical-spin-google}.../solaro-logo-colorida.png`

`reports/` é **gitignored** e contém **dados de cliente**. Copiá-lo violaria o
isolamento entre empresas (regra inegociável do projeto). **Decisão:** o installer
NÃO copia branding de `reports/`. Procura um diretório de branding *versionado*
fora de `reports/` (`branding/`, `assets/branding/`, `squads/traffic-shared/branding/`);
não existe nenhum hoje, então o script emite aviso claro e o README documenta a
colocação manual da logo ao lado do `client-presentation.html`. Ausência de branding
não bloqueia a instalação (AC 5 atendido).

### Schema das allow rules

`.claude/settings.local.json` usa `permissions.allow` com strings `Bash(cmd:*)`.
Regras de leitura adicionadas (formato confirmado lendo o arquivo do repo):
`Bash(meta-ads report:*)`, `Bash(meta-ads accounts:*)`, `Bash(meta-ads auth status)`,
`Bash(google-ads report:*)`, `Bash(google-ads accounts:*)`, `Bash(google-ads auth status)`.
Merge via `node` inline (idempotente, com backup `.bak`).

### Smoke test (Task 5) — resultado

Ambiente: `/tmp/teste-traffic-kit` (`git init`), installer rodado com `N` nas prompts de CLI.

| Validação | Resultado |
|-----------|-----------|
| 3 squads copiados (traffic-shared/meta/google) | ✓ |
| 2 shims copiados (trafficMeta/trafficGoogle) | ✓ |
| `reports/` criado vazio (só `.gitkeep`) | ✓ |
| `settings.local.json` com 6 allow rules de leitura | ✓ |
| ZERO client-profiles reais (só `_TEMPLATE.md`/`README.md`) | ✓ |
| Nenhum `solaro-logo*.png` / dados de `reports/` vazaram | ✓ |
| Run 2x — idempotência (0 regras duplicadas, 3 squad dirs) | ✓ |
| Merge preserva regra de mutação existente (`Bash(meta-ads create:*)`) | ✓ |
| `bash -n` (syntax) | ✓ (shellcheck indisponível na máquina) |

Diretórios temporários removidos ao final.

## File List

**Criados:**
- `scripts/install-traffic-kit.sh`
- `README-TRAFFIC-KIT.md`

**Modificados:**
- `docs/stories/epics/epic-8-traffic-kit-export/8.4.traffic-kit-installer.story.md`

## Change Log

| Data | Versão | Descrição | Autor |
|------|--------|-----------|-------|
| 2026-06-12 | 0.1 | Story criada a partir do Epic 8 | @sm (River) |
| 2026-06-12 | 1.0.0 | Validada GO (9/10) — Status: Draft → Approved; Task 5 mapeada aos AC 1/7/8 (antes "DoD do epic") | @po (Pax) |
| 2026-06-12 | 1.1.0 | Installer + README implementados; smoke test real ✓ (8 validações); Status: Approved → Ready for Review | @dev (Dex) |
| 2026-06-12 | 1.2.0 | QA gate PASS — Status: Ready for Review → Done | @qa (Quinn) |

## QA Results

**Gate decision:** PASS — 2026-06-12 — @qa (Quinn)

RE-executei o smoke test completo do zero em `/tmp/qa-traffic-kit` (não confiei no relato do @dev):

- `bash -n scripts/install-traffic-kit.sh` → sintaxe OK.
- `git init` + `install-traffic-kit.sh <destino> --yes`: copia `traffic-shared`, `traffic-meta`, `traffic-google`; shims `trafficMeta`/`trafficGoogle`; `reports/` vazio só com `.gitkeep`. CLIs já no PATH → installer **não tentou reinstalar** (detectou `~/.npm-global/bin/`), exatamente como esperado.
- **Isolamento no destino:** únicos client-profiles copiados são `_TEMPLATE.md` + `README.md` por squad; grep por dados reais de cliente no destino → **zero**.
- **Settings merge:** `.claude/settings.local.json` gerado com 6 allow rules de leitura. Teste de merge com arquivo preexistente → regra custom `Bash(ls:*)` e `customKey` preservados.
- **Idempotência:** 2ª execução → `0 regras adicionadas, 6 já presentes` (allow count 6, todas únicas); backup `.bak` criado.
- **Ativação real:** `.claude/commands/trafficMeta/agents/campaign-launcher.md` referencia `squads/traffic-meta/agents/campaign-launcher.md` — confirmado que esse path **existe no destino**. Shim ativável.
- README-TRAFFIC-KIT.md cobre: instalação em projeto novo (seção 2), máquina nova/auth (seção 3 com `auth status`/`auth login`), MCP `claude_ai_Facebook` (seção 4), client-profile por projeto (seção 5), troubleshooting de PATH/npm/nvm (seção 6).

`/tmp/qa-traffic-kit` limpo ao final. Sem retrabalho.

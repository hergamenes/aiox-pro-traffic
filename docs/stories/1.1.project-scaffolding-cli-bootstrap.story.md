# Story 1.1: Project Scaffolding & CLI Bootstrap

## Status

Ready for Review

## Executor Assignment

```yaml
executor: "@dev"
quality_gate: "@architect"
quality_gate_tools: ["lint", "typecheck", "test"]
```

## Story

**As a** gestor de tráfego,
**I want** ter o projeto inicializado com estrutura organizada e um comando CLI funcional,
**so that** eu tenha a base onde toda a automação de campanhas Meta Ads será construída.

## Acceptance Criteria

1. Pacote `packages/meta-ads-agent/` criado com `package.json`, `tsconfig.json` e dependências base (TypeScript, commander, pino)
2. Comando `meta-ads` executável via `npx` ou `node` que exibe versão e help
3. Estrutura de pastas criada: `src/cli/`, `src/meta-api/`, `src/creative/`, `src/campaign/`, `src/config/`, `src/auth/`, `src/log/`, `src/errors/`, `src/types/`
4. Logger (pino) configurado com output formatado para terminal (pino-pretty)
5. Script `npm run dev` funcional para desenvolvimento com watch mode
6. Script `npm run build` compila TypeScript sem erros
7. Testes configurados com vitest e pelo menos 1 teste de sanidade passando

## 🤖 CodeRabbit Integration

> **CodeRabbit Integration**: Disabled
>
> CodeRabbit CLI is not enabled in `core-config.yaml`.
> Quality validation will use manual review process only.
> To enable, set `coderabbit_integration.enabled: true` in core-config.yaml

## Tasks / Subtasks

- [x] **Task 1: Initialize package** (AC: 1)
  - [x] Create `packages/meta-ads-agent/` directory
  - [x] Initialize `package.json` with name `@aiox/meta-ads-agent`, version `0.1.0`, `"type": "module"`
  - [x] Install production dependencies: `commander@13`, `pino@9`, `pino-pretty@13`, `zod@3.24`, `yaml@2`
  - [x] Install dev dependencies: `typescript@5.7`, `tsup@8`, `vitest@3`, `eslint@9`, `prettier@3`, `@typescript-eslint/eslint-plugin`, `@typescript-eslint/parser`
  - [x] Create `tsconfig.json` with `strict: true`, `target: ES2022`, `module: NodeNext`, path alias `@/*` → `src/*`
  - [x] Create `tsup.config.ts` for CLI build (entry: `src/cli/index.ts`, format: esm, sourcemap: true)
  - [x] Create `eslint.config.js` with flat config + TypeScript rules (ESLint 9 flat config instead of `.eslintrc.cjs`)
  - [x] Create `.prettierrc` with defaults

- [x] **Task 2: Create folder structure** (AC: 3)
  - [x] Create all source directories per architecture source tree:
    - `src/cli/commands/`
    - `src/campaign/strategies/`
    - `src/creative/`
    - `src/meta-api/`
    - `src/auth/`
    - `src/config/`
    - `src/log/`
    - `src/errors/`
    - `src/types/`
  - [x] Add placeholder `.gitkeep` in each empty directory

- [x] **Task 3: Create CLI entry point** (AC: 2)
  - [x] Create `bin/meta-ads.ts` with shebang `#!/usr/bin/env node`
  - [x] Create `src/cli/index.ts` — register commander program with name `meta-ads`, version from package.json, description
  - [x] Add `--version` and `--help` flags (commander provides these by default)
  - [x] Add `bin` field in `package.json`: `{ "meta-ads": "./dist/bin/meta-ads.js" }`
  - [x] Verify `meta-ads --help` and `meta-ads --version` work after build

- [x] **Task 4: Configure logger** (AC: 4)
  - [x] Create `src/cli/logger.ts` — initialize pino with:
    - Transport: `pino-pretty` in development (`NODE_ENV !== 'production'`)
    - JSON output in production
    - Redact config: `['accessToken', 'appSecret', 'token', 'headers.authorization', '*.access_token', '*.app_secret']`
    - Level: `debug` in dev, `info` in production
  - [x] Export singleton logger instance
  - [x] Add `META_ADS_DEBUG` env var to force debug level

- [x] **Task 5: Configure build scripts** (AC: 5, 6)
  - [x] Add script `"build": "tsup"` in package.json
  - [x] Add script `"dev": "tsup --watch"` in package.json
  - [x] Add script `"lint": "eslint src/"` in package.json
  - [x] Add script `"test": "vitest run"` in package.json
  - [x] Add script `"test:watch": "vitest"` in package.json
  - [x] Verify `npm run build` compiles without errors
  - [x] Verify `npm run dev` starts watch mode

- [x] **Task 6: Setup tests and write sanity test** (AC: 7)
  - [x] Create `vitest.config.ts` with path alias `@/` → `src/`, globals: true
  - [x] Create `tests/unit/cli/index.test.ts`:
    - Test: "should display version from package.json"
    - Test: "should display help text with program name meta-ads"
  - [x] Verify `npm test` runs and passes

- [x] **Task 7: Final verification**
  - [x] Run `npm run lint` — no errors
  - [x] Run `npm run build` — no errors
  - [x] Run `npm test` — all tests pass
  - [x] Run `node dist/bin/meta-ads.js --help` — displays help
  - [x] Run `node dist/bin/meta-ads.js --version` — displays 0.1.0

## Dev Notes

### Source Tree [Source: docs/architecture.md#Source Tree]

```
packages/meta-ads-agent/
├── src/
│   ├── cli/
│   │   ├── index.ts                # Entry point — registers commands
│   │   ├── commands/               # Individual command files
│   │   ├── prompts.ts              # Interactive prompts (inquirer) — Story 3.1
│   │   └── display.ts              # Output formatting — Story 3.2
│   ├── campaign/
│   │   ├── orchestrator.ts         # Campaign Orchestrator — Story 2.3
│   │   ├── naming.ts               # Naming Service — Story 2.3
│   │   └── strategies/             # Campaign type strategies — Story 2.3, 2.4
│   ├── creative/                   # Creative Module — Story 2.1
│   ├── meta-api/                   # Meta API Adapter — Story 2.2
│   ├── auth/                       # Auth Module — Story 1.2
│   ├── config/                     # Config Repository — Story 1.3
│   ├── log/                        # Log Repository — Story 3.4
│   ├── errors/                     # Error Handler — Story 3.3
│   └── types/                      # Shared TypeScript interfaces
├── tests/
├── bin/
│   └── meta-ads.ts                 # Shebang entry point
├── package.json
├── tsconfig.json
├── tsup.config.ts
├── vitest.config.ts
├── .eslintrc.cjs
└── .prettierrc
```

### Tech Stack (this story only) [Source: docs/architecture.md#Tech Stack]

| Technology | Version | Purpose |
|-----------|---------|---------|
| Node.js | 22.x LTS | Runtime |
| TypeScript | 5.7.x | Language |
| commander | 13.x | CLI parsing |
| pino | 9.x | Structured logging |
| pino-pretty | 13.x | Dev log formatting |
| zod | 3.24.x | Validation |
| yaml | 2.x | Config file parsing |
| tsup | 8.x | Build/bundle |
| vitest | 3.x | Testing |
| eslint | 9.x | Linting |
| prettier | 3.x | Formatting |

### Coding Standards [Source: docs/architecture.md#Coding Standards]

- **ESM modules:** `"type": "module"` in package.json
- **Strict TypeScript:** `"strict": true` in tsconfig
- **Path aliases:** `@/*` → `src/*` (avoid `../../../` imports)
- **File naming:** kebab-case (`bundle-builder.ts`)
- **Max 200 lines per file**
- **Async/await everywhere** — never `.then()/.catch()`
- **Pino redact:** Always configure redact for sensitive fields

### Testing [Source: docs/architecture.md#Test Strategy]

- **Framework:** vitest 3.x
- **Location:** `tests/unit/` mirrors `src/`, `tests/integration/` by flow
- **Naming:** `{module}.test.ts`
- **Pattern:** AAA (Arrange, Act, Assert)
- **Coverage target:** 80%+ overall
- **This story:** Only sanity tests for CLI entry point

### Important Notes

- This is a **greenfield project** — no existing code to integrate with
- `npm link` will be used later to make `meta-ads` globally available (not part of this story)
- Dependencies for later stories (`facebook-nodejs-business-sdk`, `keytar`, `sharp`, `inquirer`, etc.) should NOT be installed now — only what's needed for this story
- The `src/types/` directory should contain empty placeholder files that will be populated in later stories

## Change Log

| Date | Version | Description | Author |
|------|---------|-------------|--------|
| 09-03-26 | 0.1 | Initial story draft | River (SM) |

## Dev Agent Record

### Agent Model Used
Claude Opus 4.6 (claude-opus-4-6)

### Debug Log References
- Build output: `npm run build` — ESM build success in 4ms
- Lint output: `npm run lint` — 0 errors
- Test output: `npm test` — 2 tests passed (134ms)
- CLI help: `node dist/bin/meta-ads.js --help` — displays usage
- CLI version: `node dist/bin/meta-ads.js --version` — displays 0.1.0

### Completion Notes List
- Used ESLint 9 flat config (`eslint.config.js`) instead of deprecated `.eslintrc.cjs` — ESLint 9.x uses flat config by default
- Added `typescript-eslint` and `@eslint/js@9` as additional dev dependencies for flat config support
- Added `@types/node` as dev dependency for Node.js type definitions
- tsup entry includes both `bin/meta-ads.ts` and `src/cli/index.ts`
- Logger uses conditional transport (pino-pretty only in dev, JSON in production)

### File List
| File | Status | Description |
|------|--------|-------------|
| `packages/meta-ads-agent/package.json` | Created | Package manifest with dependencies and scripts |
| `packages/meta-ads-agent/tsconfig.json` | Created | TypeScript config (strict, ES2022, NodeNext, path aliases) |
| `packages/meta-ads-agent/tsup.config.ts` | Created | Build config (ESM, node22 target, sourcemaps) |
| `packages/meta-ads-agent/eslint.config.js` | Created | ESLint 9 flat config with TypeScript rules |
| `packages/meta-ads-agent/.prettierrc` | Created | Prettier formatting config |
| `packages/meta-ads-agent/vitest.config.ts` | Created | Vitest config with path aliases and globals |
| `packages/meta-ads-agent/bin/meta-ads.ts` | Created | CLI shebang entry point |
| `packages/meta-ads-agent/src/cli/index.ts` | Created | Commander program registration (name, version, description) |
| `packages/meta-ads-agent/src/cli/logger.ts` | Created | Pino logger with pino-pretty transport and redact config |
| `packages/meta-ads-agent/tests/unit/cli/index.test.ts` | Created | Sanity tests (version + help text) |
| `packages/meta-ads-agent/src/cli/commands/.gitkeep` | Created | Placeholder |
| `packages/meta-ads-agent/src/campaign/strategies/.gitkeep` | Created | Placeholder |
| `packages/meta-ads-agent/src/creative/.gitkeep` | Created | Placeholder |
| `packages/meta-ads-agent/src/meta-api/.gitkeep` | Created | Placeholder |
| `packages/meta-ads-agent/src/auth/.gitkeep` | Created | Placeholder |
| `packages/meta-ads-agent/src/config/.gitkeep` | Created | Placeholder |
| `packages/meta-ads-agent/src/log/.gitkeep` | Created | Placeholder |
| `packages/meta-ads-agent/src/errors/.gitkeep` | Created | Placeholder |
| `packages/meta-ads-agent/src/types/.gitkeep` | Created | Placeholder |

## QA Results
_(To be filled by QA agent)_
